---
# 1. Очищення конфліктних репозиторіїв
- name: Remove all conflicting repository files
  file:
    path: "{{ item }}"
    state: absent
  with_items:
    - /etc/apt/sources.list.d/docker.list
    - /etc/apt/sources.list.d/download_docker_com_linux_ubuntu.list
    - /etc/apt/sources.list.d/kubernetes.list

# 2. Оновлення кешу та встановлення ВСІХ системних залежностей одним махом
- name: Update apt and install core dependencies
  apt:
    name:
      - curl
      - wget
      - apt-transport-https
      - ca-certificates
      - gnupg
      - lsb-release
      - software-properties-common
      - conntrack  # Потрібно для Kubernetes мережі
      - socat      # Потрібно для портів
      - iproute2   # Потрібно для драйвера none
    state: present
    update_cache: yes
    force_apt_get: yes

# 3. Встановлення crictl (вирішує помилку зі скріншота image_b7ee88.png)
- name: Download and install crictl
  shell: |
    VERSION="v1.30.0"
    curl -L "https://github.com/kubernetes-sigs/cri-tools/releases/download/${VERSION}/crictl-${VERSION}-linux-amd64.tar.gz" --output crictl.tar.gz
    tar zxvf crictl.tar.gz -C /usr/local/bin
    rm -f crictl.tar.gz
  args:
    creates: /usr/local/bin/crictl

# 4. Підготовка груп та користувача (для порядку в системі)
- name: Ensure docker group exists
  group:
    name: docker
    state: present

- name: Create devops user
  user:
    name: devops
    shell: /bin/bash
    groups: docker,sudo
    append: yes
    create_home: yes

- name: Allow devops user to use sudo without password
  copy:
    content: "devops ALL=(ALL) NOPASSWD:ALL"
    dest: /etc/sudoers.d/devops
    mode: '0440'

- name: Install cri-dockerd
  shell: |
    # Визначаємо останню версію
    VERSION="0.3.15"
    # Завантажуємо архів
    curl -L https://github.com/Mirantis/cri-dockerd/releases/download/v${VERSION}/cri-dockerd-${VERSION}.amd64.tgz --output cri-dockerd.tgz
    tar zxvf cri-dockerd.tgz
    # Копіюємо бінарник
    install -o root -g root -m 0755 cri-dockerd/cri-dockerd /usr/local/bin/cri-dockerd
    # Завантажуємо та налаштовуємо системні сервіси (systemd)
    wget https://raw.githubusercontent.com/Mirantis/cri-dockerd/master/packaging/systemd/cri-docker.service
    wget https://raw.githubusercontent.com/Mirantis/cri-dockerd/master/packaging/systemd/cri-docker.socket
    mv cri-docker.service cri-docker.socket /etc/systemd/system/
    sed -i -e 's,/usr/bin/cri-dockerd,/usr/local/bin/cri-dockerd,' /etc/systemd/system/cri-docker.service
    # Запускаємо сервіс
    systemctl daemon-reload
    systemctl enable --now cri-docker.socket
    systemctl enable --now cri-docker.service
    # Чистимо сміття
    rm -rf cri-dockerd.tgz cri-dockerd
  args:
    creates: /usr/local/bin/cri-dockerd


