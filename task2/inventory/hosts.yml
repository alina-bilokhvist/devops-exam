all:
  vars:
    ansible_user: root
    ansible_ssh_private_key_file: /tmp/exam_key
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
  hosts:
    node:
      ansible_host: "{{ lookup('env', 'VM_IP') }}"   # IP VM береться зі змінної середовища


